#compdef praxrail pxr
# Generated by scripts/generate-cli-artifacts.ts
_praxrail() {
  if (( CURRENT == 2 )); then
    _values 'command' version start stop restart status logs chat interactive repl ask command cmd watch output shell init login logout doctor runtime profile project repo task channel approval upgrade support
    return
  fi
  case "$words[2]" in
      runtime) _values 'action' serve start stop restart status logs ;;
      profile) _values 'action' list use ;;
      project) _values 'action' create list show update archive ;;
      repo) _values 'action' add inspect approve list show disable remove ;;
      task) _values 'action' create list show clarify prioritize pause resume cancel retry abandon archive status watch logs events attempts costs verification findings ownership attach shell return recover diff check review fix publish pull-request ;;
      channel) _values 'action' setup link verify status test preference rotate disable revoke ;;
      approval) _values 'action' approve reject ;;
      upgrade) _values 'action' preflight ;;
      support) _values 'action' bundle ;;
  esac
}
_praxrail "$@"
